apiVersion: v2
name: istiod
description: Istio control plane (istiod) — manages the Envoy sidecar data plane
type: application
version: 1.0.0
appVersion: "1.23.0"

dependencies:
  - name: istiod
    version: 1.23.x
    repository: https://istio-release.storage.googleapis.com/charts
