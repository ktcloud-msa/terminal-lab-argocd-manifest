apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
    app.kubernetes.io/part-of: terminal-lab
# The frontend does not talk to the API server — don't mount a token.
automountServiceAccountToken: false
